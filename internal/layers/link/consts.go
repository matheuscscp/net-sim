package link

const (
	// MTU (maximum transmission unit) is the maximum number of bytes that are
	// accepted on the payload of a frame (the link layer name for a packet).
	MTU = 1500

	// MaxQueueSize is the maximum size of the link layer channels.
	MaxQueueSize = 1024
)
